<h4 class="aside-section-heading">
  {{heading}}
</h4>
<ul class="aside-section-list">
  {{#each items}}
  <li>
    <span class="color-dark">{{name}}</spa>
    <span class="badge-grey">{{level}}</span>
  </li>
  {{/each}}
</ul>